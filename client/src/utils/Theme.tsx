import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0055FF",
    },
    secondary: {
      main: "#10D876",
    },
    info: {
      main: "#343a40",
    },
    // tertiary: {}
    text: {
      primary: "#111",
    },
  },
});

export default theme;
